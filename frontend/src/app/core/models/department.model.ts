export interface Department {
  departmentId: string;
  departmentName: string;
  departmentCode: string;
  headName: string;
  building: string;
  phoneNumber: string;
  email: string;
  totalFaculty: number;
  totalStudents: number;
  createdDate: string;
}
